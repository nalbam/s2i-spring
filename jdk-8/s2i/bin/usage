#!/bin/sh

cat <<EOF
This is an S2I java running with openjdk-1.8 compiling with maven-3.5.3 centos base image responsible for consuming a java maven project
and deploying it onto Spring Boot application server.
To use it, install S2I: https://github.com/openshift/source-to-image.

Sample invocation:

s2i build git://github.com/bespin-sbl/sample-spring bespinsbl/s2i-springboot:8-mvn-3.5 sample-spring

You can then run the resulting image via:
docker run -p 8080:8080 sample-spring

EOF
